#include <stdio.h>
#include <stdlib.h>

int main() {
    int n;
    
    // Read the size of the array
    if (scanf("%d", &n) != 1) {
        return 1;
    }
    
    // Dynamically allocate memory for n integers
    int *arr = (int*)malloc(n * sizeof(int));
    
    // Check if memory allocation was successful
    if (arr == NULL) {
        return 1;
    }
    
    int sum = 0;
    
    // Read values into the array and accumulate the sum
    for (int i = 0; i < n; i++) {
        if (scanf("%d", &arr[i]) == 1) {
            sum += arr[i];
        }
    }
    
    // Print the final sum
    printf("%d\n", sum);
    
    // Free the dynamically allocated memory
    free(arr);
    
    return 0;
}
