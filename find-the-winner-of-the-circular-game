#include <stdio.h>

int findTheWinner(int n, int k) {
    int winner = 0; // Base case for 1 person (0-indexed)
    
    // Bottom-up evaluation from 2 to n people
    for (int i = 2; i <= n; i++) {
        winner = (winner + k) % i;
    }
    
    return winner + 1; // Convert to 1-indexed
}
